{
  "name": "mentorvoice",
  "version": "1.0.0",
  "description": "Voice cloning and text-to-speech mentorship app",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
}
